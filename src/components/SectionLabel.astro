---
interface Props {
  number?: string;
  text: string;
}
const { number, text } = Astro.props;
---

<p class="section-label">{number && <><span>{number}</span><span aria-hidden="true"> / </span></>}{text}</p>
